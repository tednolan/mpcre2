;
; pcre2setcompilerecursionguard
;
; This function is currently untestable as we have no way to create a
; recursion guard function from within M.  If you happen to have such
; a function pointer laying around, it should work, but no promises.
;
; For now we just say the function succeeds.

	write "0",!
	quit
